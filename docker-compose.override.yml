services:
  site-monitor:
    # Monta templates e assets estáticos para desenvolvimento rápido
    volumes:
      - ./templates:/app/templates:delegated
      - ./static:/app/static:delegated

    # Alternativa (descomentar) para montar o projeto inteiro e usar o reloader do Flask:
    # volumes:
    #   - ./:/app:delegated
    # environment:
    #   - FLASK_APP=app.py
    #   - FLASK_ENV=development
    # command: python -m flask run --host=0.0.0.0 --port=5000 --reload
